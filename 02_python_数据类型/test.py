# ls.append(x) 列表最后添加一个元素x
ls = ["刘伟", 20, "张三", 30, "李四"]
ls.append("小白")
print(ls)

# ls.insert(index,x) 在列表ls的第index位置增加一个元素x
# index  使用索引来定位
ls.insert(1, "小粉")
print(ls)

# ls.clear() 清空列表中的元素
    # ls.clear(ls)
    # print(ls)

